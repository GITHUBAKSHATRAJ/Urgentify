import { Platform } from 'react-native';

// API Endpoint configuration for the mobile client.
const getApiBaseUrl = () => {
  return 'https://urgentify.onrender.com/api';
};

export const API_BASE_URL = getApiBaseUrl();
